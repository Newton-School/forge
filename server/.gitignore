node_modules
# Anchor to the server's OWN build output only — do NOT ignore the vendored
# prebuilt SDK at vendor/newton-auth/dist (it must be committed for CI builds).
/dist
.env
.env.*
!.env.example
*.log
.DS_Store

# Secrets (service-account keys, GitHub App .pem, etc.) — never commit
secrets/
*.pem
certs/

# Testing Portal config — local only (contains real tester emails/roster). Never commit.
testing-portal.json
testing-portal.*.json
!testing-portal.example.json
